import { objectName, objectType } from "./Object";
import defaultObject from "./DefaultObject";
import def from "./Default";

const { defaultObjectName, defaultObjectType } = defaultObject;

export { objectName, objectType, defaultObjectName, defaultObjectType, def };

export default def;
