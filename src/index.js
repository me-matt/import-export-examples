import def, {
  objectName,
  objectType,
  defaultObjectName,
  defaultObjectType
} from "./Values";

console.log(`object :         name=${objectName}, type=${objectType}`);
console.log(
  `default object : name=${defaultObjectName}, type=${defaultObjectType}`
);
console.log(`default : ${def}`);
